


    // if (inputNumber.length === 1) {
    //   result = getNumberInArabic(arr, inputNumber, 0, 0);
    // }

    // if (inputNumber.length === 2) {
    //   let and = "";
    //   if (parseInt(inputNumber) > 19 && parseInt(inputNumber[1]) !== 0) {
    //     and = "و";
    //   }
    //   let first = "";
    //   if (parseInt(inputNumber[0]) > 0) {
    //     first = getNumberInArabic(arr, inputNumber, 0, 1);
    //   }
    //   let second = "";
    //   if (parseInt(inputNumber[1]) > 0) {
    //     second = getNumberInArabic(arr, inputNumber, 1, 0);
    //   }
    //   let end = "";
    //   if (parseInt(inputNumber[0]) === 1 && parseInt(inputNumber[1]) === 0)
    //     end = local.teh;

    //   result = `${second} ${and} ${first}${end}`;
    // }